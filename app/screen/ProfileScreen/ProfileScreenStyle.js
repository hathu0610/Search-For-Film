import {StyleSheet} from 'react-native'
export const styles = StyleSheet.create({
    container:{
        flex:1,
        padding:20,
        alignItems:'center'
    },
    textheading:{
        color: '#ffbb3b',
        fontSize: 30,
        fontWeight:'bold',
        marginTop:30,
        fontFamily:'Georgia'
      },
      circle: {
        marginTop:87,
        width: 200,
        height: 200,
        borderRadius: 200/2,
        resizeMode:'contain'
     }
});